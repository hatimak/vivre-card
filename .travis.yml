sudo: require

dist: trusty

before_install:
  - sudo apt-get -qq update

install:
  - sh ./texlive_install.sh

before_script:
  - sh ./test_texlive.sh

script:
  - xelatex test.tex
  # - make cv.pdf

# cache:
#   directories:
#     - /tmp/texlive
#     - $HOME/.texlive

branches:
  only:
    - new

# notifications:
#   email:
#     recipients:
#       - hatim@hatimak.me
#     on_success: change
#     on_failure: always
