sudo: require

dist: trusty

before_install:
  - sudo apt-get -qq update

install:
  - sh ./texlive_install.sh

script:
  - make cv.pdf

cache:
  directories:
    - /tmp/texlive

branches:
  only:
    - new

notifications:
  email:
    recipients:
      - hatim@hatimak.me
    on_success: change
    on_failure: always
